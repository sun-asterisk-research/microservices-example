type Query {
  hello: String!
}

type Mutation {
  login(
    credentials: LoginInput!
  ): LoginOutput
}

input LoginInput {
  username: String!
  password: String!
}

type LoginOutput {
  accessToken: String!
}

