actions:
  - name: hello
    definition:
      kind: ""
      handler: http://api:8080
      request_transform:
        method: GET
        query_params: {}
        request_headers:
          add_headers: {}
          remove_headers:
            - content-type
        template_engine: Kriti
        url: '{{$base_url}}/hello'
        version: 2
      response_transform:
        body:
          action: transform
          template: '{{$body.message}}'
        template_engine: Kriti
        version: 2
    comment: Just say "Hello world!"
  - name: login
    definition:
      kind: synchronous
      handler: http://api:8080/login
      timeout: 5
custom_types:
  enums: []
  input_objects:
    - name: LoginInput
  objects:
    - name: LoginOutput
  scalars: []
